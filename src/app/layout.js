import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "SSC Bento Grid Dashboard | Seemanchal SmartVyapaar Consultancy",
  description: "A modern, responsive Bento Grid dashboard UI built for Seemanchal SmartVyapaar Consultancy. Features interactive analytics, revenue goals, recent activity logs, real-time team lists, and schedule planning.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
